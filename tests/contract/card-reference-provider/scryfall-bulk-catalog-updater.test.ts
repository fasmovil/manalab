import assert from 'node:assert/strict'; import test from 'node:test'; import { mkdtemp, rm } from 'node:fs/promises'; import { tmpdir } from 'node:os'; import { join } from 'node:path'; import { gzipSync } from 'node:zlib';
import { LocalCatalogStore } from '../../../src/adapters/card-reference/local-catalog-store.js'; import { ScryfallBulkCatalogUpdater } from '../../../src/adapters/card-reference/scryfall-bulk-catalog-updater.js';
const manifest = { id: 'v2', type: 'oracle_cards', updated_at: '2026-02-01T00:00:00Z', jsonl_download_uri: 'https://fixture/data', compressed_size: 10 };
test('updates newer catalog then avoids unchanged download', async () => { const dir = await mkdtemp(join(tmpdir(), 'manalab-')); let calls = 0; const fetcher: typeof fetch = async (url) => { calls++; return String(url).includes('bulk-data') ? new Response(JSON.stringify({ data: [manifest] })) : new Response(gzipSync('{"name":"Sol Ring"}\n')); }; try { const updater = new ScryfallBulkCatalogUpdater(new LocalCatalogStore(dir), fetcher); assert.equal(await updater.updateIfNewer(), 'updated'); assert.equal(await updater.updateIfNewer(), 'already-current'); assert.equal(calls, 3); } finally { await rm(dir, { recursive: true, force: true }); } });
test('retains usable catalog after manifest failure', async () => { const dir = await mkdtemp(join(tmpdir(), 'manalab-')); try { const store = new LocalCatalogStore(dir); await store.replace([{ name: 'Sol Ring' }], { provider: 'fixture', datasetKind: 'oracle_cards', providerDatasetId: 'v1', providerUpdatedAt: 'old', sourceDownloadUri: 'fixture', declaredSize: 1, acquiredAt: 'now' }); const updater = new ScryfallBulkCatalogUpdater(store, async () => new Response('', { status: 503 })); assert.equal(await updater.updateIfNewer(), 'update-unavailable-local-usable'); assert.ok(await store.load()); } finally { await rm(dir, { recursive: true, force: true }); } });
test('reports no usable catalog when acquisition fails without local data', async () => { const dir = await mkdtemp(join(tmpdir(), 'manalab-')); try { const updater = new ScryfallBulkCatalogUpdater(new LocalCatalogStore(dir), async () => new Response('', { status: 503 })); assert.equal(await updater.updateIfNewer(), 'no-usable-catalog'); } finally { await rm(dir, { recursive: true, force: true }); } });
test('refreshes when consistency fields change', async () => { const dir = await mkdtemp(join(tmpdir(), 'manalab-')); const changed = { ...manifest, jsonl_download_uri: 'https://fixture/new', compressed_size: 11 }; const fetcher: typeof fetch = async url => String(url).includes('bulk-data') ? new Response(JSON.stringify({ data: [changed] })) : new Response(gzipSync('{"name":"Sol Ring"}\n')); try { const store = new LocalCatalogStore(dir); await store.replace([{ name: 'Old Card' }], { provider: 'Scryfall', datasetKind: 'oracle_cards', providerDatasetId: 'v2', providerUpdatedAt: manifest.updated_at, sourceDownloadUri: manifest.jsonl_download_uri, declaredSize: 10, acquiredAt: 'now' }); assert.equal(await new ScryfallBulkCatalogUpdater(store, fetcher).updateIfNewer(), 'updated'); } finally { await rm(dir, { recursive: true, force: true }); } });
test('retains the active catalog when a streamed record is malformed', async () => { const dir = await mkdtemp(join(tmpdir(), 'manalab-')); const fetcher: typeof fetch = async url => String(url).includes('bulk-data') ? new Response(JSON.stringify({ data: [manifest] })) : new Response(gzipSync('{"name":"Sol Ring"}\n{"name":42}\n')); try { const store = new LocalCatalogStore(dir); await store.replace([{ name: 'Old Card' }], { provider: 'fixture', datasetKind: 'oracle_cards', providerDatasetId: 'v1', providerUpdatedAt: 'old', sourceDownloadUri: 'fixture', declaredSize: 1, acquiredAt: 'now' }); assert.equal(await new ScryfallBulkCatalogUpdater(store, fetcher).updateIfNewer(), 'update-unavailable-local-usable'); assert.deepEqual((await store.load())?.cards, [{ name: 'Old Card' }]); } finally { await rm(dir, { recursive: true, force: true }); } });
