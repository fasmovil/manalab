import assert from 'node:assert/strict'; import test from 'node:test';
import { importCommanderDeck } from '../../../src/application/import-commander-deck.js';
import { LocalCatalogCardReferenceProvider } from '../../../src/adapters/card-reference/local-catalog-card-reference-provider.js';
import { loadCatalog } from '../../../src/adapters/card-reference/local-catalog-loader.js';
const provider = new LocalCatalogCardReferenceProvider(loadCatalog({ cards: [{ name: 'Sol Ring' }, { name: 'Winota, Joiner of Forces' }], provenance: { provider: 'fixture', datasetKind: 'oracle_cards', providerDatasetId: 'v1', providerUpdatedAt: 'now', sourceDownloadUri: 'fixture', declaredSize: 1, acquiredAt: 'now' } }));
test('preserves Commander intent but treats Ramp as neutral metadata', async () => { const result = await importCommanderDeck('Commander\n1 Winota, Joiner of Forces\nDeck\n1 Sol Ring [Ramp]', provider); assert.equal(result.resolvedContent.find(c => c.identity.canonicalName === 'Winota, Joiner of Forces')?.commanderDesignated, true); assert.equal(result.resolvedContent.find(c => c.identity.canonicalName === 'Sol Ring')?.commanderDesignated, false); });
test('preserves Commanders-section and inline category metadata', async () => { const result = await importCommanderDeck('Commanders\n1 Winota, Joiner of Forces\nDeck\n1 Winota, Joiner of Forces [Commander{top}]\n1 Sol Ring [Ramp]', provider); const winota = result.resolvedContent.find(content => content.identity.canonicalName === 'Winota, Joiner of Forces')!; const solRing = result.resolvedContent.find(content => content.identity.canonicalName === 'Sol Ring')!; assert.equal(winota.commanderDesignated, true); assert.deepEqual(winota.sourceEntries.map(entry => entry.inlineCategoryMetadata), [[], ['Commander{top}']]); assert.deepEqual(solRing.sourceEntries[0].inlineCategoryMetadata, ['Ramp']); });
